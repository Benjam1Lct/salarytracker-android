package com.benjamin.salarytracker.ui.theme

import androidx.compose.ui.graphics.Color

// ── Palette Notivo : lavande clair + accent violet ───────────────────────────
val Lavender     = Color(0xFFF2EEFB)   // fond de page
val LavenderAlt  = Color(0xFFEAE3F8)   // zones/chart/surfaceVariant
val CardWhite    = Color(0xFFFFFFFF)

val Purple       = Color(0xFF8B7CF6)   // primary
val PurpleSoft   = Color(0xFFC9BEF2)
val PurpleDeep   = Color(0xFF6D5BD0)
val PurpleTint   = Color(0xFFEDE9FE)

val Ink          = Color(0xFF14121A)   // texte principal
val InkMuted     = Color(0xFF8B8896)   // texte secondaire
val OutlineLt    = Color(0xFFE8E2F5)

// ── Widget sombre (uniformisé sur le gris de la carte contrat) ───────────────
val WidgetGray   = Color(0xFF1F1D26)   // gris sombre (référence)
val WidgetDark   = WidgetGray          // plus de noir : même gris partout
val WidgetTrack  = Color(0xFF2A2731)
val OnWidget     = Color(0xFFF4F2F8)
val OnWidgetMut  = Color(0xFF9A95A6)

// ── Sémantique ────────────────────────────────────────────────────────────────
val PosGreen     = Color(0xFF16A34A)
val NegRed       = Color(0xFFEF4444)
val AmberAccent  = Color(0xFFF59E0B)

// ── Dock ────────────────────────────────────────────────────────────────────
val NavBarLight  = Color(0xFFFFFFFF)
val NavBarDark   = Color(0xFF111016)
